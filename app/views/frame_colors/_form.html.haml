= form_for @frame_color do |f|
  - if @frame_color.errors.any?
    #error_explanation
      %h2= "#{pluralize(@frame_color.errors.count, "error")} prohibited this frame_color from being saved:"
      %ul
        - @frame_color.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
